<nav class="navbar navbar-expand-lg navbar-light bg-dark fixed-top">
    <div class="container-lg mx-auto">
        <ul class="d-flex justify-content-between w-100 navbar-nav">
            <li class="nav-item">
                <a class="nav-link font-bold fs-5 text-white" href="/">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link font-bold fs-5 text-white" href="/my-cart">
                    My Cart 
                    <i class="far fa-shopping-cart inline-flex">
                        <div class="text-xs text-white px-1 rounded-circle bg-danger mt-2 -ms-1">
                            <% 
                                let NumOfCartItems = 0
                                cartItems.forEach(item => {
                                    NumOfCartItems += item.quantity
                                })
                            %>
                            <span id="NumOfItems"> <%= NumOfCartItems %> </span>
                        </div>
                    </i>
                </a>
            </li>
        </ul>
    </div>
</nav>
